{
  "name": "cheq-rti-fastly-compute",
  "version": "1.0.0",
  "description": "CHEQ RTI translation layer for Fastly VCL integration",
  "main": "src/index.js",
  "scripts": {
    "build": "fastly compute build",
    "deploy": "fastly compute deploy",
    "test": "jest"
  },
  "devDependencies": {
    "@fastly/js-compute": "^3.0.0",
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
