{
  "name": "rti-cdn-core",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "tsc --noEmit && vitest --reporter=verbose --watch false",
    "coverage": "vitest run --coverage --coverage.provider=istanbul"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitest/coverage-istanbul": "4.1.6",
    "typescript": "^5.9.3",
    "vitest": "4.1.6"
  }
}
