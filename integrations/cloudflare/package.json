{
    "name": "rti-cdn-integrations.cloudflare",
    "version": "1.0.0",
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20251221.0",
        "prettier": "^3.7.4",
        "typescript": "^5.9.3",
        "vitest": "^4.0.16",
        "wrangler": "^4.56.0"
    },
    "private": true,
    "scripts": {
        "start": "wrangler dev",
        "deploy": "npm run test && wrangler deploy",
        "test": "tsc && vitest --reporter=verbose --watch false"
    }
}
